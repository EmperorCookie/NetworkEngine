///ne_enums(do_not_call)

enum NET {
    UDP_PORT,
    DISCONNECT,
    PING,
    TIME,
    TICK_RATE,
    CREATE,
    DESTROY,
    CREATE_ID,
    SYNC_ENTITY,
    SYNC_REQUEST,
    ZONE_SET,
    ZONE_SUBSCRIBE,
    ZONE_UNSUBSCRIBE
}

enum SYNC {
    PROPERTIES,
    MOVEMENT,
    PHYSICS,
    VARIABLES
}

